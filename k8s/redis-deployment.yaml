apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketplace-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: marketplace-redis
  template:
    metadata:
      labels:
        app: marketplace-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: marketplace-redis
spec:
  selector:
    app: marketplace-redis
  ports:
    - port: 6379
      targetPort: 6379