spring:
  application:
    name: distributed-marketplace-platform

  datasource:
    url: jdbc:mysql://localhost:3306/marketplace
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

server:
  port: 8080