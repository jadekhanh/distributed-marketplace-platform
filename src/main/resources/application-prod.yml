spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: INFO
    org.hibernate.SQL: WARN
    org.springframework.security: WARN

aws:
  region: us-east-2
  s3:
    bucket: jade-and-plushies-gang-marketplace